import { PreventEDirective } from './prevent-e.directive';

describe('PreventEDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventEDirective();
    expect(directive).toBeTruthy();
  });
});
